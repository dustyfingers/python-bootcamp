# there are rules about where variables can be accessed from

# vars created in a function are scoped to that function
# try to minimize the number of global variables used!

# nonlocal keyword
# like 'global' but instead of allwing us to access vars in the global scope,
# it allows us access to vars created in a parent functions scope
